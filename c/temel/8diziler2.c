#include <stdio.h>
#include <string.h>
int main()
{
	char isim[]={"bilge"};
	printf("%s",isim);
	printf("%c",isim[0]);
	
	
	
	
	
	//include <string.h> kütüphanesi kullanýlarak yapýlabilecekler;
	//strcat (bir stringi diðer stringin sonuna ekler)
	//strlen (string karakter sayýsýný boyutunu belirler)
	//strcmp (ayný mý deðil mi diye kontrol eder)
	//strlwr (diziyi küçük harfe çevirir)
	//strupr (diziyi büyük harfe çevirir)
	//strrev (diziyi ters çevirir)
	//strncmp (belirli bir n karakter limitine kara 2 diziyi karþýlaþtýrýr)
	//strpcpy (bir karakter dizisini baþka bir karakter dizisine kopyalar)
	//strncpy (bir karakter dizisini baþka bir karakter dizisine n karakter limite kadar kopyalar)
	//strchr (karakter dizisinde bir karakteri arar)
	//strstr (karakter dizisinde bir alt karakter dizisi arar)
	return 0;
}
